mod_id=artemis
mod_name=Artemis
mod_group=fr.natsu.artemis
mod_version=0.1.0
mod_author=["Natsu91"]
mod_description=Forge -> Apollo (Lunar Client) compatibility for Minecraft 1.8.9

minecraft_version=1.8.9
forge_version=11.15.1.2318-1.8.9
mappings_version=stable_22

mod_mixin_configs=mixins.${mod_id}.json
mod_mixin_refmap=refmap.${mod_id}.json

org.gradle.daemon=false
org.gradle.jvmargs=-Xmx3G

# Forge 1.8.9 / ForgeGradle 2.x must run on a JDK 8 toolchain.
# Set org.gradle.java.home to your local JDK 8 path (keep it out of git, e.g. in ~/.gradle/gradle.properties):
# org.gradle.java.home=/path/to/jdk-8
